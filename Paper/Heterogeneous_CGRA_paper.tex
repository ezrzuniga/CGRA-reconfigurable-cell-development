% IEEE-formatted preliminary article (conference)
\documentclass[conference]{IEEEtran}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{csquotes}
\usepackage{url}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{adjustbox}
\usepackage{rotating}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{cite}
\usepackage{hyperref}
\usepackage{tikz}
\usetikzlibrary{positioning, fit, arrows.meta, calc, patterns, decorations.pathreplacing}

\title{Development of a Heterogeneous Reconfigurable CGRA Cell}
\author{\IEEEauthorblockN{Duarte Sanchez David, Jimenez Ulate Eddy, Ruiz Rivera Jeffrey, Esquivel Arias Fabian, Zúñiga Ramírez Jose Eduardo}
\IEEEauthorblockA{School of Electronics Engineering \\
Costa Rica Institute of Technology \\
\{davidduarte28, e.jimenez.10, yr1ruiz, fabiesqui22, je.zuniga\}@estudiantec.cr}
}
\date{}

\begin{document}
\maketitle

% Loosen line breaking to avoid overfull boxes in two-column layout
\sloppy




%-----------------------------------------------
%       Abstract
%-----------------------------------------------

\begin{abstract}

Coarse-Grained Reconfigurable Arrays (CGRAs) are gaining interest in the industry due to the flexibility they provide and the facility to program them. 
There are different architectures of CGRAs, being the most common the homogeneous CGRA but these present a problem of underutilization of resources when the workload is not uniform.
This work presents a heterogeneous CGRA cell architecture composed of five different elements: a routing cell, distributed memory cell, scalar cell, vector cell and a MAC cell.
The design was implemented in SystemC and validated with Vitis HLS, using two main kernels: GEMM and sum-reduction.
For GEMM, the spatial mapping reaches higher throughput (33.74 MOP/s vs 13.56 MOP/s) and lower latency per operation (29.64 ns/op vs 73.72 ns/op), at the cost of higher resource usage (up to 4.58$\times$ FF and 4.00$\times$ DSP). 
For sum-reduction, the temporal MAC-based mapping achieves better efficiency, with higher utilization (79.7\% vs 47.8\%) and lower latency (50.95 ns/op vs 55.65 ns/op), while also requiring fewer resources. 
These results support the central claim that heterogeneous, workload-aware CGRA composition improves performance-resource trade-offs compared with uniform PE deployment.

\end{abstract}

\begin{IEEEkeywords}
coarse-grained reconfigurable architecture (CGRA), heterogeneous CGRA, spatial CGRA, reconfigurable computing.
\end{IEEEkeywords}


%-----------------------------------------------
%       Introduction
%-----------------------------------------------

\section{Introduction}


Reconfigurable architectures have been very successful in industry because software evolution has advanced rapidly, with diverse applications,
different user needs, and scientific progress, so hardware cannot adapt at the same pace as software \cite{LeiboLiu}. Special-purpose systems
can suffer from rapid obsolescence, and if the price of hardware development and production is high, it becomes economically unviable \cite{LeiboLiu}.

Currently, architectures such as FPGAs (Field Programmable Gate Arrays) have been widely adopted in diverse applications due to their flexibility and customization capability, gaining traction in HPC (High Performance Computing).
However, FPGAs present certain limitations such as slow configuration times, long compilation times, and in cases, low operating frequencies \cite{Podobas}.

In the recent years there has been growing interest in HPC in using architectures such as CGRAs (Coarse-Grained Reconfigurable Arrays), which offer an intermediate solution between FPGAs and ASICs (Application Specific Integrated Circuits).
CGRAs are hardware architectures that, conceptually, allow silicon to be malleable and their functionality to be dynamically reconfigured \cite{Podobas}.
CGRAs have the advantage of being one to two orders of magnitude more efficient than FPGAs and more than 2 to 3 orders of magnitude more efficient than ASICs \cite{LeiboLiu}.

The development of a reconfigurable CGRA cell is of great importance not only for its relevance in HPC, but also for its adoption in Machine-Learning applications, image processing, and computer vision,
communications, and other areas \cite{dvhetero}. Therefore, the objective of this work is to develop a heterogeneous CGRA with different kind of processing elements such as a scalar and vector processing cells, memory banks and a MAC (Multiply-Accumulate) unit. 



%-----------------------------------------------
%       Background and Related Work
%-----------------------------------------------

\section{Background and Related Work}
In recent years, CGRAs have gained significant prominence as hardware accelerators due to their ability to offer an exceptional balance between software flexibility and the high performance of application-specific hardware \cite{LeiboLiu,Podobas}. 
Unlike fine-grained architectures, CGRAs operate at the word or block level, which substantially reduces reconfiguration and routing overhead, making them particularly attractive for efficient edge-computing applications. 
The development and research of these architectures has been strongly driven by the adoption of the open-source RISC-V instruction set architecture (ISA) \cite{riscv}, whose extensible nature facilitates the creation of highly customized heterogeneous SoCs (Systems on Chip).

At the level of practical implementation, these architectures are the result of cutting-edge open-source efforts. 
Repositories such as risc-v-cgra (developed by ECASLab) investigate these integrations at the system level.
OpenCGRA is a unified framework that supports the full top-to-bottom design flow, this adopts a Python-based PyMTL3 framework to model the architecture and enabling the RTL generation and multi-level simulation \cite{OpenCGRA}
TRAM is another example which adopts a Chisel-based CGRA template to model hardware architecture with register transfer level (RTL) code generation and functional-level (FL) simulation; TRAM proposes a flexible and scalable CGRA template composed of GPEs, IOBs and GIBs with two-dimensional (2D) island-style topology \cite{TRAM}. 
HETA is a temporal CGRA implementing a heterogeneous architecture with a Bayesian optimization, this CGRA is a 2D island style layout built from four type of blocks: GPE (Generic Processing Element), GIB (General Interconnect Block), LSU (Load/Store Unit) and IOB (I/O Block) \cite{HETA}. 

For this type of system to reach its full potential, the internal design of the individual reconfigurable cell (PE) and its associated compilation tools are critical \cite{satyajit}. 
They highlight the need for context-memory-aware mapping to ensure energy-efficient acceleration, emphasizing that data-access latency dictates overall performance. 
Within this framework, the design and development of a new reconfigurable CGRA cell is grounded in the need to optimize local interconnects, deterministic access to intermediate operands, and the versatility of its cycle-level operations — challenges that remain areas of active exploration at the frontier of heterogeneous accelerator design.


%-----------------------------------------------
%       SoTA Analysis
%-----------------------------------------------

\section{Analysis of Existing Solutions (SOTA)} %Jose
\label{sec:sota}

Nowadays there are different types of CGRAs, they can be classified by their elements in heterogeneous or homogeneous, also based on how the data is handled, they can be classified as temporal or spatial. 
Examples of homogeneous CGRAs are the HyCUBE, DySER, TRAM and OpenCGRA \cite{Blocks2022, TRAM, OpenCGRA} which have an array of homogeneous or equal PEs, while examples of heterogeneous CGRAs are Plasticine and HETA \cite{plasticine2017, HETA} which combine different PE.

CGRA systems are no longer seen as an isolated element; today they are presented as a heterogeneous part of an architecture system \cite{dvhetero}.
Furthermore, CGRAs have become highly important in HPC (High Performance Computing) systems, which greatly benefit from having heterogeneous-type PEs, with a mix of basic and complex PEs \cite{exploration_tradeoffs}. 
This is highly important for mathematical functions, since traditional CGRAs do not handle them optimally.

In Table \ref{tab:comparativa_sota} it is shown a comparison between four types of CGRAs. They present different trade-offs, where they are developed according to the area of application or workload. 
For example the HETA CGRA is good when the physical space is a concern \cite{HETA} or the Plasticine is a good option when the workload is highly parallel \cite{plasticine2017}.
\begin{table}[htbp]
\caption{Comparison of state-of-the-art solutions}
\label{tab:comparativa_sota}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\columnwidth}{!}{%
  \begin{tabular}{@{}l p{2.2cm} p{2.2cm} p{2.2cm} p{1.6cm}@{}}
  \toprule
  \textbf{Solution} & \textbf{Strengths} & \textbf{Weaknesses} & \textbf{Viability} & \textbf{PE / CGRA Type}\\
  \midrule
  HETA \cite{HETA} & Bayesian DSE requires zero manual intervention, very fast CGRA. & Only functional and RTL simulation, limited verification tooling. & Good for research, ideal for temporal CGRA exploration. & Heterogeneous / Temporal.\\
  \addlinespace
  TRAM \cite{TRAM} & High PE utilization and clean interconnect model. & Spatial CGRA only, cannot time-multiplex resources. & Good for research, when the interconnect flexibility and mapping are priorities. & Homogeneous / Spatial.\\
  \addlinespace
  OpenCGRA \cite{OpenCGRA} & Very complete, with multi-level simulation. Based on mature LLVM and PyMTL3 infrastructure. & The mapper can give a low PE utilization. Since it uses python-based modeling, large designs can be slower. & Ideal for research and prototyping, presenting a very complete verification and integration flow. & Homogeneous / Spatial.\\
  \addlinespace
  Plasticine \cite{plasticine2017} & Massive per-cycle processing (OP/s), built for parallel patterns. & Rigid programming and restrictive power consumption. & Fixed infrastructure/servers, proved at 28nm with real performance numbers. & Heterogeneous / Spatial.\\
  \bottomrule
  \end{tabular}%
}
\end{table}



%-----------------------------------------------
%       Proposed Solution and Second-Level Architecture
%-----------------------------------------------

\section{Proposed Solution and Second-Level Architecture}


Architectures like the homogeneous CGRA present a challenge where there is under-utilization of resources like PEs and interconnects \cite{adres2004, hycube2017}. 
Since all PEs are identical and have general-purpose capabilities, a large fraction of functional units remain idle during execution \cite{HETA}. 
In addition, the uniform interconnects can leave many routes unused \cite{dvhetero}.
This kind of architecture has the advantage of being simple to design and implement, but that same simplicity is a limitation when high parallelism and throughput are required. 
Such architectures are not energy-efficient and have low performance \cite{Blocks2022}.

In contrast, the heterogeneous CGRAs have different types of cells, allowing better performance and resource utilization based on the objective of the CGRA, like the HETA architecture \cite{HETA}.
The HETA architecture uses scalar PEs \cite{HETA} while the Plasticine architecture uses vector PEs \cite{plasticine2017}. Even though these architectures are heterogeneous, there is a dominant type of PE to do the operations.


This work presents a heterogeneous CGRA with different PE types, routing cells and distributed memory cells. This design is proposed in order to have specialized cells according to the necessity of the workload, having a specialized element for a wider range of operations with fewer elements. The elements of the CGRA are as follows:

\begin{itemize}
    \item \textbf{Routing:} statically-configured combinational switch (4 stored contexts).
    \item \textbf{Distributed memory:} local dual-port SRAM banks (2~KB per cell in the current model, parametrizable).
    \item \textbf{Scalar processing unit:} fixed 32-bit RV32I+MUL ALU with a single 16-instruction program.
    \item \textbf{Vector processing unit:} int32 SIMD ALU with 4 fixed lanes.
    \item \textbf{MAC processing unit:} multiply-accumulate unit with a persistent per-lane accumulator for high-throughput operations.
\end{itemize}

With these elements, we are introducing a new heterogeneous CGRA architecture that can be used for scalar operations and vector operations, also with the introduction of the MAC PE it is expected to have good performance with different specialized cells.
Currently no other architecture has been found that combines scalar and vector operations, routing cells and distributed memory cells, which makes this architecture unique in its kind.



\subsection{Architectural Proposal}
\label{sec:architectural_proposal}

This architecture proposal is shown in Figure \ref{fig:diagrama_2nivel_png}, where the different elements of the CGRA are present. In this case, it is a basic 2x3 array but this could be expanded according to the necessity.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.01\columnwidth]{images/lvl2_diagram.png}
    \caption{Second-level architecture diagram generated with artificial intelligence.}
    \label{fig:diagrama_2nivel_png}
\end{figure}

\subsubsection{Routing Cell}
Implemented as a purely combinational switch: 8 ports (4 mesh + 4 local), each output selecting any of the 8 inputs (or none) via a per-context 4-bit selector.
Concurrent readers of the same source are served as copies rather than arbitrated, giving inherent multicast fan-out. 
Four contexts are stored per cell, each loaded in a single clock edge; which context is active is chosen combinationally. 

\subsubsection{Distributed Memory Cell}
Dual-port SRAM (2~KB per cell in the current instantiation, configurable via a template parameter); direct and strided access modes; a local DMA engine performs host-free block transfers between SRAM and the NoC. 
Four independently configurable access contexts are supported. This design still eliminates centralized memory contention by giving each cell its own local bank.

\subsubsection{Scalar Cell}
Integrated by a 32-bit ALU (RV32I integer/logic ops plus MUL) with a 8~$\times$~32~b register file and a single 16-instruction program memory that the program counter loops through. 

\subsubsection{Vector Cell}
A 4-lane int32 SIMD VALU (add, sub, logic, shifts, compare, MUL, the same op set as the Scalar ALU, applied per lane) with 8~$\times$~128~b vector registers and a single 16-instruction program memory.

\subsubsection{MAC Cell}
This PE uses a VALU op set and 8~$\times$~128~b register file and a dedicated MAC opcode.
Each lane's product is computed the same way as MUL, then added in the writeback stage into a persistent per-lane accumulator (4~$\times$~32~b) that survives \texttt{rst} and is addressable like any other operand via dedicated source/destination selectors. 



\subsection{Development Tools and Environment}

The development of this heterogeneous CGRA starts with the implementation of SystemC to verify the functionality of the architecture and its elements. 
Afterwards, the SystemC model is translated so it can be used in Vitis 2024.1 and generate a RTL code and get the metrics of the design.
Also there will be a software validation using GEM5 to simulate the RISC-V core and the CSR/DMA interface, while the CGRA will be modeled in SystemC.

In order to ensure strict reproducibility of the design and simulation tests, the software tooling infrastructure and its respective versions are defined in Table \ref{tab:herramientas}.

\begin{table}[htbp]
\caption{Development Tools and Environment Versions}
\label{tab:herramientas}
\centering
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.18\linewidth}p{0.18\linewidth}p{0.62\linewidth}@{}}
\toprule
\textbf{Tool / Software} & \textbf{Version} & \textbf{Purpose in the Experiment} \\
\midrule
SystemC & 3.0.1 & Modeling and simulation of the CGRA architecture functionality. \\
Vitis HLS & 2024.1 & High-Level Synthesis (HLS) from SystemC to RTL (VHDL). \\
GEM5 & v25.1 & Simulation of the RISC-V core and CSR/DMA interface, integrated with the SystemC CGRA model. \\
%Xilinx Vivado Design Suite & 2024.1 & Logic synthesis and timing analysis. \\
%ModelSim / Questa Advanced & 2025.1 & Cycle-accurate RTL functional simulation. \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}



%-----------------------------------------------
%       Experimental Plan and Methodology
%-----------------------------------------------

\section{Experimental Plan and Methodology}%Eddy
\label{sec:experimento}


\subsection{Testing and Stimulus}


To test an electronic design, a testbench is needed, in charge of driving a stimulus into the design and facilitating monitoring of what happens in the design under test (DUT). 
As has been done in other designs, and in the various CGRA development frameworks, it becomes necessary to have an algorithm that the architecture is capable of executing. 
For this, one typically starts from so-called kernels: representative algorithms, typically coded in C (for example, Sum-Reduction and GEMM), annotated with compiler directives on their loops, whose data-flow structure can be mapped onto a CGRA's PE array. 
This is, in fact, the approach followed by Morpher within the OpenCGRA ecosystem: starting from the C kernel, it generates the data-flow graph (DFG) and the scratchpad memory layout, and produces the test vectors that feed the simulation/emulation stage, which are used to verify functional correctness and obtain area and power estimates \cite{Juneja}. 
The use of Sum-Reduction as a reference kernel is also consistent with the CGRA literature oriented toward signal processing, where it recurrently appears alongside routines such as IDCT, FIR, and IIR \cite{Podobas}.

It is worth noting that even in well-established frameworks such as OpenCGRA, the authors themselves point out that the simulator relies on testbenches manually written by the user and does not automate test-data generation or end-to-end verification \cite{Juneja}. 
This reinforces the relevance of proposing, from the outset, a structured and progressive testing flow such as the one proposed below.

While a valuable goal is to achieve end-to-end execution of an algorithm such as Sum-Reduction or GEMM compiled from start to finish (from the C program to the configuration bitstream and the CGRA's input data), given the time available for the project's development, the first objective targets algorithmically simpler tests. 
The following steps are proposed:

\begin{enumerate}
    \item \textbf{A single PE executing $c = a + b$ (constant) or $c = a \times b$.} This validates loading of the configuration word (config word), reading of registers/inputs, the ALU operation, and writing of the output result. It is, literally, the architecture's ``hello world.''
    \item \textbf{Chains of operations, in pipeline mode:} accumulated sum ($y = x_0 + x_1 + x_2 + \dots + x_n$) and successive-squaring powers ($x,\ x^2,\ x^4,\ x^8,\ \dots$).
    \item \textbf{Simple MAC ($acc \mathrel{+}= a \times b$):} this is the base block of almost every real kernel (Sum-Reduction, GEMM, convolutions), with this working correctly, the validation can scale toward more complex designs.
\end{enumerate}

Once these basic tests are passed, progress can be made toward typical benchmarks from the CGRA literature, such as MachSuite and PolyBench \cite{PolyBenchC,MachSuite}, which together with Wavelib and BEEBS make up the set of suites used by Morpher to evaluate its compilation flow \cite{Juneja}. 
These suites provide kernel libraries (including Sum-Reduction and GEMM) that can be mapped onto the CGRA through a compilation flow similar to the one described above.

For the software validation it is planned to use the softmax kernel. 
This will be executed using GEM5 to simulate the interaction between a RISC-V core and the CGRA. 

\subsection{Monitoring}

This project will be developed in SystemC, the aim is to leverage the language's own features to implement performance measurement. 
It is proposed to implement a monitor as an \texttt{SC\_MODULE} component that observes, non-intrusively, DUT elements such as signals and internal counters. 
This separation between functional logic and measurement logic follows SystemC's own modular design principle, in which the monitor can be added to or removed from the testbench without modifying the module under analysis.

It is proposed that the monitor compute, at minimum, the following metrics when evaluating the HDL post-synthesis:

\begin{itemize}
    \item \textbf{Operations per second (OP/s):} ratio between the total number of operations executed by the DUT and the total execution time.
    \item \textbf{Average latency:} recording \texttt{sc\_time\_stamp()} at the start and end of each individual operation, averaged over the sampling window.
    \item \textbf{Utilization rate:} proportion of active cycles relative to the total number of cycles.
\end{itemize}

These metrics are printed to the console via \texttt{SC\_REPORT\_INFO}.

Additionally, other metrics that will be taken into account are the quantity of:
\begin{itemize}
    \item LUT (Look-Up Table)
    \item FF (Flip-Flops)
    \item DSP (Digital Signal Processing)
    \item BRAM (Block RAM)
    \item SRL (Shift Register LUT)
\end{itemize}
These are necessary to measure the resource utilization of each design and are obtained from Vitis 2024.1 after the synthesis of the design.


Regarding the performance metrics when trying to evaluate the software integration with the CGRA, the following metrics will be taken into account for the specified kernel (softmax function):
\begin{itemize}
    \item \textbf{Test cases passed:} number of test cases that passed successfully.
    \item \textbf{Worst per-lane error (LSB):} the worst-case error in least significant bits (LSB) across all lanes.
    \item \textbf{Maximum sum deviation (LSB):} the maximum deviation from the expected sum in LSB.
    \item \textbf{Output probability sum range:} the range of the sum of output probabilities, which should be close to 1 for valid softmax outputs.
\end{itemize}




\subsection{Architectural Exploration}

Tied to design-space exploration, the plan is to generate variations of the CGRA's architectural design by modifying the number and type of PEs (\textit{processing elements}). 
For example, changes in the distribution of PE types (more memory banks, more fixed-point ALUs, more floating-point ALUs), as well as different $n \times n$ array dimensions, aimed at optimizing the execution of vector instructions.
This type of exploration is consistent with the diversity of CGRA architectures reported in the literature, where performance depends heavily on the composition and arrangement of the PEs \cite{Podobas}.





%-----------------------------------------------
%       Results and Discussion
%-----------------------------------------------
\section{Results and Discussion}

This section presents the results obtained from the implementation of the CGRA with the proposed kernels and monitoring metrics from section \ref{sec:experimento}. 



\subsection{GEMM Kernel}

The GEMM (General Matrix-Matrix Multiplication) kernel was implemented in two ways using the MAC cell, the temporal implementation and spatial implementation. 
The first one uses a single MAC cell to perform the operation, while the other one uses 4 MAC cells in total to do the same operation. 
Table \ref{tab:resource_util_GEMM} shows the resource utilization of both implementations, where as expected, the temporal implementation is cheaper in resources than the spatial implementation. 

\begin{table}[htbp]
\centering
\caption{Post-synthesis resource utilization (Vivado) for temporal vs. spatial implementations}
\label{tab:resource_util_GEMM}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.18\linewidth}p{0.18\linewidth}p{0.31\linewidth}p{0.31\linewidth}@{}}
\toprule
\textbf{Resource} & \textbf{Temporal} & \textbf{Spatial} & \textbf{Spatial/Temporal} \\
\midrule
LUT & 1199 & 4467 & 3.73$\times$ \\
FF & 1551 & 7098 & 4.58$\times$ \\
DSP & 3 & 12 & 4.00$\times$ \\
SRL & 0 & 0 & -- \\
BRAM & 0 & 0 & -- \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Regarding the performance of each configuration, Table \ref{tab:perf_metrics_gemm} shows that the spatial implementation has a better performance than the temporal implementation.
The quantity of operations per second is higher in the spatial implementation and has a lower latency per operation, since the PEs are the same, the spatial has the advantage because it has more parallelism available.

\begin{table}[htbp]
\centering
\caption{Performance metrics from measured 2$\times$2 GEMM execution}
\label{tab:perf_metrics_gemm}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.26\linewidth}p{0.20\linewidth}p{0.20\linewidth}p{0.26\linewidth}@{}}
\toprule
\textbf{Metric} & \textbf{MAC Cell - Temporal} & \textbf{MAC Cell - Spatial} & \textbf{Spatial/Temporal} \\
\midrule
Operations per second (MOP/s) & 13.56  & 33.74  & 2.49$\times$ \\
Average latency per operation (ns/op) & 73.72  & 29.64 & 0.40$\times$ \\
Utilization rate (\%) & 58.6 & 71.9 & 1.23$\times$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}


The downside of the temporal implementation is the necessity to have a start=true transaction for each invocation, while the spatial implementation only needs one invocation. 
This represents a total of 204 cycles vs 82 cycles of the spatial implementation, in Table \ref{tab:rtl_transaction_cycles_gemm} are the values of each implementation.

\begin{table}[htbp]
\centering
\caption{Per-transaction RTL cycle counts from cosimulation (actual simulated hardware)}
\label{tab:rtl_transaction_cycles_gemm}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.17\linewidth}p{0.20\linewidth}p{0.16\linewidth}p{0.24\linewidth}p{0.17\linewidth}@{}}
\toprule
\textbf{Design} & \textbf{\# prog\_valid transactions} & \textbf{Cost each} & \textbf{start=true transaction cost} & \textbf{Outputs per start=true} \\
\midrule
Temporal & 3 & 12 cycles & \textbf{51 cycles} & 1 ($C[i][j]$) \\
Spatial & 16 & 2 cycles & \textbf{82 cycles} & 4 (whole matrix) \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}



\subsection{Sum-Reduction Kernel}

For the sum-reduction kernel two comparisons were done, one temporal implementation with our new MAC cell and another spatial implementation with 4 scalar cells. 
Both of the solutions were implemented under the same hardware considerations, using a Kria K26 SoM as reference with a 250 MHz clock target.
The setup for this validation is shown in Table \ref{tab:sum_setup}.

\begin{table}[htbp]
\centering
\caption{Temporal vs. spatial sum-reduction setup}
\label{tab:sum_setup}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.22\linewidth}p{0.48\linewidth}p{0.26\linewidth}@{}}
\toprule
\textbf{Item} & \textbf{Temporal} & \textbf{Spatial} \\
\midrule
Mesh & \texttt{MAC PE} & \texttt{Scalar PE} $\times$ 4 \\
Cell count & 1 & 4 \\
\texttt{NUM\_PHASES} & 8 & 1 \\
\texttt{INSTR\_MEM\_SIZE} & 3 & 3 \\
Test vectors & \texttt{\{6,-2,9,4,0,7,-5,3\}} = 22, \texttt{\{100,-55,30,7,-12,4,-9,15\}} = 80 & same \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}


The results of these implementations are shown in Table \ref{tab:resource_util_sumReduction}. 
The new MAC cell shows a better utilization compared with the standard solution given by the literature, like the TRAM solution \cite{TRAM}. 

\begin{table}[htbp]
\centering
\caption{Post-synthesis resource utilization for Sum-Reduction kernel}
\label{tab:resource_util_sumReduction}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.18\linewidth}p{0.18\linewidth}p{0.31\linewidth}p{0.31\linewidth}@{}}
\toprule
\textbf{Resource} & \textbf{MAC PE - Temporal} & \textbf{Scalar PE - Spatial} & \textbf{Spatial/Temporal} \\
\midrule
LUT & 1205 & 3857 & 3.20$\times$ \\
FF & 1551 & 5230 & 3.37$\times$ \\
DSP & 3 & 12 & 4.00$\times$ \\
SRL & 0 & 20 & -- \\
BRAM & 0 & 0 & -- \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}


Regarding the performance metrics, Table \ref{tab:perf_metrics_sumReduction} shows that the temporal implementation with the MAC cell shows a better performance since this kind cell is designed to perform the sum-reduction operation in a more efficient way that a normal scalar cell. 
With this results it is possible to confirm the importance of having a heterogeneous CGRA with specialized cells for specific operations, since this allows to have a better performance and a better resource utilization.


\begin{table}[htbp]
\centering
\caption{Performance metrics from measured sum-reduction execution}
\label{tab:perf_metrics_sumReduction}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.26\linewidth}p{0.20\linewidth}p{0.20\linewidth}p{0.26\linewidth}@{}}
\toprule
\textbf{Metric} & \textbf{MAC PE - Temporal} & \textbf{Scalar PE - Spatial} & \textbf{Spatial/Temporal} \\
\midrule
Operations per second (MOP/s) & 19.63 & 17.97 & 0.92$\times$ \\
Average latency per operation (ns/op) & 50.95  & 55.65  & 1.09$\times$ \\
Utilization rate (\%) & 79.7 & 47.8 & 0.60$\times$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Regarding the software validation for this kernel, GEM5 was used to simulate the interaction between an ARM core and the CGRA. 
Table \ref{tab:gem5_results_sumReduction} shows the cycle counts for the implementation, which are matching the RTL cosimulation results. 

%\begin{table}[htbp]
%\centering
%\caption{GEM5/RTL co-simulation cycle counts for the sum-reduction kernel.}
%\label{tab:gem5_rtl_cycles_sumReduction}
%\scriptsize
%\begin{adjustbox}{max width=\columnwidth}
%\begin{tabular}{@{}p{0.31\linewidth}p{0.30\linewidth}p{0.31\linewidth}@{}}
%\toprule
%\textbf{Design} & \textbf{Cycles} & \textbf{Matches RTL cosim?}  \\
%\midrule
%Temporal CGRA & 141 & Yes --- exact match  \\
%Spatial CGRA & 154 & Yes --- exact match  \\
%Both designs, one round (141+154) & 295 & derived  \\
%Both designs, both rounds & 590 & derived  \\
%\bottomrule
%\end{tabular}
%\end{adjustbox}
%\end{table}

Also Table \ref{tab:gem5_results_sumReduction} shows the results of the GEM5 co-simulation for the sum-reduction kernel, where the output of the operation was the expected one.
With this results it is possible to confirm that the drivers to interact with the CGRA are working correctly.

\begin{table}[htbp]
\centering
\caption{GEM5 co-simulation results for the sum-reduction kernel}
\label{tab:gem5_results_sumReduction}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.32\linewidth}p{0.18\linewidth}p{0.08\linewidth}p{0.10\linewidth}p{0.08\linewidth}p{0.09\linewidth}p{0.15\linewidth}@{}}
\toprule
\textbf{Test vector} & \textbf{Design} & \textbf{total} & \textbf{expected} & \textbf{cycles} & \textbf{Result} & \textbf{CPU models} \\
\midrule
\multirow{2}{*}{\{6,-2,9,4,0,7,-5,3\}}
  & Temporal CGRA & 22 & 22 & 141 & PASS & atomic + o3 \\
  & Spatial CGRA  & 22 & 22 & 154 & PASS & atomic + o3 \\
\addlinespace
\multirow{2}{*}{\{100,-55,30,7,-12,4,-9,15\}}
  & Temporal CGRA & 80 & 80 & 141 & PASS & atomic + o3 \\
  & Spatial CGRA  & 80 & 80 & 154 & PASS & atomic + o3 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}


\subsection{Softmax Kernel}

For the validation of the softmax kernel, the proposed heterogeneous CGRA was tested using GEM5 to simulate the RISC-V core and the CSR/DMA interface, while the CGRA was modeled in SystemC.
In this validation, the vector unit performed the parallel reduction, exponential, and normalization stages, while the scalar unit computed the reciprocal term $1/S$ using a Newton--Raphson approximation. 

The softmax kernel was also validated end-to-end on the proposed heterogeneous CGRA. 
Six representative test cases were executed with different input flavors: uniform inputs, dominant-lane cases, negative fractional inputs, and a saturation scenario with input spread beyond the useful exponential range to verify the CGRA accuracy in computation terms of such algorithm. 
In Table \ref{tab:softmax_validation} the summary of the validation is shown, where all the test cases passed successfully, with an error below the acceptance threshold


\begin{table}[htbp]
\centering
\caption{Validation summary for end-to-end softmax execution}
\label{tab:softmax_validation}
\scriptsize
\begin{adjustbox}{max width=\columnwidth}
\begin{tabular}{@{}p{0.42\linewidth}p{0.24\linewidth}p{0.24\linewidth}@{}}
\toprule
\textbf{Metric} & \textbf{Observed} & \textbf{Acceptance / Comment} \\
\midrule
Test cases passed & 6/6 & All passed \\
Worst per-lane error (LSB) & 2.81 & $< 6.00$ LSB \\
Maximum sum deviation (LSB) & 4.00 & $\leq 8.00$ LSB \\
Output probability sum range & 0.999023 -- 0.999512 & Close to 1 \\
Ordering preserved & Yes & In all test cases \\
Dominant-lane case & Passed & Stable behavior \\
Saturation case & Passed & Stable under large spread \\
%Execution flow & End-to-end PASS & RISC-V $\rightarrow$ CSR/DMA $\rightarrow$ MainMemory $\rightarrow$ MeshWrapper \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

These results show that the proposed heterogeneous organization can correctly support more complex nonlinear kernels beyond GEMM and sum-reduction, while maintaining good numerical accuracy under both nominal and extreme input conditions.


%-----------------------------------------------
%       Conclusions
%-----------------------------------------------

\section{Conclusions}

In this work we propose a heterogeneous CGRA with different types of cells, including a new MAC PE that has not been mentioned in past works. 
This work shows that having different types of cells in a CGRA architecture can translate in a better performance. 
The design of a CGRA depends on the type of workload that is going to be executed, but for general purposes the heterogeneous architecture with specialized cells presents a better solution for performance.
Also the type of architecture: spatial or temporal is very important to take into consideration because of the space available to implement the CGRA, the spatial is going to use more resources. 
It is important to confirm not only the hardware functionality but also the software integration with the CGRA, since this is a key point to have a complete solution.


%-----------------------------------------------
%       Future Work
%-----------------------------------------------

\section{Future Work}

This work is the first step in the development of a heterogeneous CGRA, so there is still work that can be done to improve the architecture and validation of the design. 
The architecture can be increased in complexity by implementing a large number of PEs, this needs a more complex communication between the different cells but improving the capacity of the CGRA.
Also adding more variety of PEs can improve the performance of the CGRA, since depending on the workload, the more suitable PE can be used to improve the performance. 
In terms of validation, the CGRA can be tested with more complex kernels and also can be emulated in an FPGA to get more accurate results.


%-----------------------------------------------
%       AI Tool Usage Declaration
%-----------------------------------------------

\section*{AI Tool Usage Declaration}

All the content presented in this work was determined by the authors of this project, using tools like Gemini, GitHub Copilot and Claude as supportive resources.
These tools were used to help with the clarification of concepts and ideas, to improve the structure of the documents as well as the grammar of it.
Finally, the formatting of figures was generated using the assistance of AI tools. 


\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}



%https://github.com/ECASLab/risc-v-cgra/tree/main

%https://github.com/esl-epfl/OpenEdgeCGRA/tree/main

%https://github.com/pnnl/OpenCGRA
